/**
 * Pagination Helper
 * SOLID: Single Responsibility - Handle pagination operations
 */
import type { InfiniteScrollConfig } from "../../domain/types/infinite-scroll-config";
import type { InfiniteScrollState } from "../../domain/types/infinite-scroll-state";
/**
 * Sleep utility for retry delay
 */
export declare function sleep(ms: number): Promise<void>;
/**
 * Retry logic with exponential backoff
 */
export declare function retryWithBackoff<T>(fn: () => Promise<T>, maxRetries: number, baseDelay: number): Promise<T>;
export declare function createInitialState<T>(initialPage: number, totalItems?: number): InfiniteScrollState<T>;
export declare function isCursorMode<T>(config: InfiniteScrollConfig<T>): config is Extract<InfiniteScrollConfig<T>, {
    paginationMode: "cursor";
}>;
export declare function loadData<T>(config: InfiniteScrollConfig<T>, pageOrCursor: number | string | undefined, pageSize: number, totalItems?: number): Promise<InfiniteScrollState<T>>;
export declare function loadMoreData<T>(config: InfiniteScrollConfig<T>, state: InfiniteScrollState<T>, pageSize: number): Promise<Partial<InfiniteScrollState<T>>>;
