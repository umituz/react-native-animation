/**
 * Carousel Calculations
 *
 * Utility functions for carousel calculations.
 * Note: Pass screenWidth from useScreenWidth() hook to ensure
 * reactivity on orientation changes, iPad Split View, etc.
 */
export declare const calculateItemWidth: (screenWidth: number, padding?: number) => number;
export declare const calculateIndexFromScroll: (scrollPosition: number, itemWidth: number) => number;
