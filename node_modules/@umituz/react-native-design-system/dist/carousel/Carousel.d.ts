import React from "react";
import { ViewStyle } from "react-native";
import type { CarouselProps } from "./types";
export declare const Carousel: <T>({ items, renderItem, itemWidth, spacing, onIndexChange, showDots, pagingEnabled, style, }: CarouselProps<T> & {
    style?: ViewStyle;
}) => React.JSX.Element | null;
