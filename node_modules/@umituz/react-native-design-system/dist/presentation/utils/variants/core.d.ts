import { ViewStyle, TextStyle, ImageStyle } from 'react-native';
export type Style = ViewStyle | TextStyle | ImageStyle;
export interface VariantConfig<T extends Record<string, Record<string, Style>>> {
    base?: Style;
    variants?: T;
    defaultVariants?: Partial<{
        [K in keyof T]: keyof T[K];
    }>;
}
export type VariantProps<T extends Record<string, Record<string, Style>>> = {
    [K in keyof T]?: keyof T[K];
};
export declare function createVariants<T extends Record<string, Record<string, Style>>>(config: VariantConfig<T>): (props?: VariantProps<T>) => Style;
