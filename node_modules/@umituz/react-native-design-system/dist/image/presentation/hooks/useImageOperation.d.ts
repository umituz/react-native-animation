/**
 * Presentation - Image Operation Hook
 *
 * Generic state management for async image operations
 */
export declare const useImageOperation: () => {
    isProcessing: boolean;
    error: string | null;
    execute: <T>(operation: () => Promise<T>, errorMessage: string) => Promise<T | null>;
};
