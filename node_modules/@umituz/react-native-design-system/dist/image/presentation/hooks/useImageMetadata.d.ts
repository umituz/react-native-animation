/**
 * Presentation - Image Metadata Hook
 */
import { type ImageMetadataExtractionOptions } from '../../infrastructure/services/ImageMetadataService';
export declare const useImageMetadata: () => {
    extractMetadata: (uri: string, options?: ImageMetadataExtractionOptions) => Promise<import("../..").ImageMetadataExtended | null>;
    getBasicInfo: (uri: string) => Promise<{
        format: string;
        size: number;
        dimensions: {
            width: number;
            height: number;
        };
    } | null>;
    hasMetadata: (uri: string) => Promise<boolean | null>;
    isExtracting: boolean;
    metadataError: string | null;
};
