/**
 * Infrastructure - Style Filters
 *
 * Vintage and blur effects
 */
export declare class StyleFilters {
    static applyVintage(data: Uint8ClampedArray, intensity: number, warmth: number): Uint8ClampedArray;
    static applyBlur(data: Uint8ClampedArray, radius: number, width: number, height: number): Uint8ClampedArray;
}
