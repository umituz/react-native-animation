/**
 * Image Infrastructure - Metadata Extractor Utilities
 *
 * Helper functions for extracting metadata from images.
 * NOTE: These methods require a native image library (e.g. expo-image-manipulator,
 * expo-file-system) to be implemented. They throw until a real implementation is provided.
 */
export interface ExifData {
    DateTimeOriginal?: string;
    Make?: string;
    Model?: string;
    ISO?: number;
    FocalLength?: number;
    Flash?: boolean;
    ExposureTime?: string;
    FNumber?: number;
}
export declare class MetadataExtractor {
    static getImageDimensions(_uri: string): Promise<{
        width: number;
        height: number;
    }>;
    static getFileSize(_uri: string): Promise<number>;
    static extractExifData(_uri: string): Promise<ExifData | null>;
    static extractGPSData(_uri: string): Promise<{
        latitude: number;
        longitude: number;
    } | null>;
    static detectFormat(uri: string): string;
}
