/**
 * Infrastructure - Filter Processor
 *
 * Filter processing with preset management
 */
export interface FilterPreset {
    id: string;
    name: string;
    category: 'basic' | 'color' | 'artistic' | 'vintage';
    parameters: FilterParameter[];
    preview?: string;
}
export interface FilterParameter {
    name: string;
    type: 'slider' | 'color' | 'boolean';
    min?: number;
    max?: number;
    value: number | string | boolean;
    step?: number;
    label: string;
}
export interface FilterState {
    id: string;
    intensity: number;
    parameters: Record<string, number>;
    enabled: boolean;
}
export declare class FilterProcessor {
    private static readonly PRESETS;
    static getPresets(category?: string): FilterPreset[];
    static getPreset(id: string): FilterPreset | undefined;
    static createFilterState(presetId: string): FilterState;
    static applyFilter(imageData: Uint8ClampedArray, width: number, height: number, filterState: FilterState): Uint8ClampedArray;
}
