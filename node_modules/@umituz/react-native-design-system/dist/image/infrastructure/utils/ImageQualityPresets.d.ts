/**
 * Image Infrastructure - Quality Presets
 *
 * Predefined quality settings for different use cases
 */
import type { SaveFormat } from '../../domain/entities/ImageTypes';
export interface QualityPreset {
    format: SaveFormat;
    quality: number;
    maxWidth?: number;
    maxHeight?: number;
    description: string;
}
export interface QualityPresets {
    web: QualityPreset;
    mobile: QualityPreset;
    print: QualityPreset;
    thumbnail: QualityPreset;
    preview: QualityPreset;
    archive: QualityPreset;
}
export declare const IMAGE_QUALITY_PRESETS: QualityPresets;
export declare class ImageQualityPresetService {
    static getPreset(name: keyof QualityPresets): QualityPreset;
    static getAllPresets(): QualityPresets;
    static getCustomPreset(options: {
        format?: SaveFormat;
        quality?: number;
        maxWidth?: number;
        maxHeight?: number;
    }): QualityPreset;
    static optimizeForUseCase(useCase: 'web' | 'mobile' | 'print' | 'thumbnail' | 'preview' | 'archive', customOptions?: Partial<QualityPreset>): QualityPreset;
}
