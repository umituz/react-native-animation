/**
 * Image Infrastructure - Batch Processor Utilities
 *
 * Helper functions for batch processing operations
 */
import type { ImageManipulationResult } from '../../domain/entities/ImageTypes';
import type { BatchOperation } from '../types/BatchTypes';
export declare class BatchProcessor {
    static processBatchItem(operation: BatchOperation): Promise<{
        uri: string;
        result: ImageManipulationResult | null;
        error?: Error;
    }>;
    static processBatchInChunks<T>(items: T[], processor: (item: T) => Promise<unknown>, concurrency: number, onProgress?: (completed: number, total: number) => void): Promise<void>;
}
