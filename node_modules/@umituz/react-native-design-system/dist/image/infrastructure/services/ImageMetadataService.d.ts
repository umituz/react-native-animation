/**
 * Image Infrastructure - Metadata Service
 *
 * Extracts and manages image metadata including EXIF data
 */
import type { ImageMetadataExtended } from '../../domain/entities/ImageFilterTypes';
export interface ImageMetadataExtractionOptions {
    includeExif?: boolean;
    includeGPS?: boolean;
    includeCamera?: boolean;
}
export declare class ImageMetadataService {
    static extractMetadata(uri: string, options?: ImageMetadataExtractionOptions): Promise<ImageMetadataExtended>;
    static getBasicInfo(uri: string): Promise<{
        format: string;
        size: number;
        dimensions: {
            width: number;
            height: number;
        };
    }>;
    static hasMetadata(uri: string): Promise<boolean>;
}
