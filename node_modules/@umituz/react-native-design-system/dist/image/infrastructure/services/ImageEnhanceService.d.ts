/**
 * Image Infrastructure - Enhance Service
 *
 * AI-powered image enhancement and automatic adjustments
 */
import type { ImageManipulationResult } from '../../domain/entities/ImageTypes';
import type { ImageQualityMetrics, ImageColorAdjustment } from '../../domain/entities/ImageFilterTypes';
export interface AutoEnhancementOptions {
    enhanceBrightness?: boolean;
    enhanceContrast?: boolean;
    enhanceColor?: boolean;
    reduceNoise?: boolean;
    sharpen?: boolean;
    targetQuality?: number;
}
export interface EnhancementResult {
    originalMetrics: ImageQualityMetrics;
    enhancedMetrics: ImageQualityMetrics;
    appliedAdjustments: ImageColorAdjustment;
    improvementScore: number;
}
export declare class ImageEnhanceService {
    static analyzeImage(uri: string): Promise<ImageQualityMetrics>;
    static autoEnhance(uri: string, options?: AutoEnhancementOptions): Promise<EnhancementResult>;
    static enhancePortrait(uri: string): Promise<ImageManipulationResult>;
    static enhanceLandscape(uri: string): Promise<ImageManipulationResult>;
}
