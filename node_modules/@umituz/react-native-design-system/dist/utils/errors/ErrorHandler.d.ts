/**
 * ErrorHandler
 *
 * Centralized error handling utility for the design system.
 * Provides consistent error handling, logging, and reporting.
 */
import { DesignSystemError, type ErrorMetadata } from './DesignSystemError';
import type { Result } from './types/Result';
export declare class ErrorHandler {
    /**
     * Handle and normalize errors
     * Converts any error type to DesignSystemError
     */
    static handle(error: unknown, context?: string, additionalContext?: Record<string, any>): DesignSystemError;
    /**
     * Log error to console (only in development)
     */
    static log(error: DesignSystemError | Error | unknown): void;
    /**
     * Handle error with logging
     * Combines handle() and log() for convenience
     */
    static handleAndLog(error: unknown, context?: string, additionalContext?: Record<string, any>): DesignSystemError;
    /**
     * Create a new DesignSystemError with specific code
     */
    static create(message: string, code: string, context?: Record<string, any>): DesignSystemError;
    /**
     * Wrap async function with error handling
     * Returns [error, result] tuple (similar to Go pattern)
     */
    static tryAsync<T>(fn: () => Promise<T>, context?: string): Promise<[DesignSystemError | null, T | null]>;
    /**
     * Wrap sync function with error handling
     * Returns [error, result] tuple
     */
    static try<T>(fn: () => T, context?: string): [DesignSystemError | null, T | null];
    /**
     * Normalize any error to DesignSystemError with metadata
     */
    static normalize(error: unknown, code: string, metadata?: ErrorMetadata): DesignSystemError;
    /**
     * Wrap async function with timeout
     */
    static withTimeout<T>(promise: Promise<T>, timeoutMs: number, context?: string): Promise<T>;
    /**
     * Wrap async function returning Result type
     */
    static tryAsyncResult<T>(fn: () => Promise<T>, code: string, metadata?: ErrorMetadata): Promise<Result<T, DesignSystemError>>;
    /**
     * Wrap sync function returning Result type
     */
    static tryResult<T>(fn: () => T, code: string, metadata?: ErrorMetadata): Result<T, DesignSystemError>;
}
