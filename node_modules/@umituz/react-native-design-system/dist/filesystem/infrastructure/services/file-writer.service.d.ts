/**
 * File Writer Service
 * Single Responsibility: Write files to device storage
 */
import type { FileEncoding, FileOperationResult } from "../../domain/entities/File";
/**
 * Write string to file
 */
export declare function writeFile(uri: string, content: string, encoding?: FileEncoding): Promise<FileOperationResult>;
