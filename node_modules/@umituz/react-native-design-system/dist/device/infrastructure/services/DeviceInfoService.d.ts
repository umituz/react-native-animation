/**
 * Device Info Service
 *
 * Single Responsibility: Get device information from native modules.
 * Uses expo-device (optional peer dep) with safe fallback.
 */
import type { DeviceInfo } from '../../domain/entities/Device';
export declare class DeviceInfoService {
    private static cachedDeviceInfo;
    private static cachePromise;
    static getDeviceInfo(): Promise<DeviceInfo>;
    private static fetchDeviceInfo;
    static clearCache(): void;
    private static getMinimalDeviceInfo;
}
