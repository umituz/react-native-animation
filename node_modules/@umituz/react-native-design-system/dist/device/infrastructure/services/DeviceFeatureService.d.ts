/**
 * Device Feature Service
 *
 * Generic service for tracking device-based feature usage.
 * Apps configure feature limits via props, service tracks usage.
 *
 * @domain device
 * @layer infrastructure/services
 */
import type { DeviceFeatureConfig, DeviceFeatureAccess } from '../../domain/entities/DeviceFeatureConfig';
export declare class DeviceFeatureService {
    private static config;
    private static inMemoryUsage;
    private static dirtyFeatures;
    private static flushInterval;
    private static FLUSH_DELAY;
    static setConfig(config: DeviceFeatureConfig): void;
    /**
     * Start periodic flush of in-memory usage to storage
     */
    private static startPeriodicFlush;
    /**
     * Flush dirty features to storage
     */
    private static flushDirtyFeatures;
    /**
     * Stop periodic flush (call on app cleanup)
     */
    static destroy(): Promise<void>;
    static checkFeatureAccess(featureName: string): Promise<DeviceFeatureAccess>;
    static incrementFeatureUsage(featureName: string): Promise<void>;
    private static getFeatureUsage;
    private static setFeatureUsage;
    private static resetFeatureUsage;
    private static shouldResetUsage;
    private static calculateNextReset;
    private static getStorageKey;
}
