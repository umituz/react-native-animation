/**
 * Offline Store
 * Manages network connectivity state across the application
 * Uses expo-network for universal network detection
 */
import type { OfflineState, OfflineActions } from '../../types';
export declare const useOfflineStore: import("zustand").UseBoundStore<import("zustand").StoreApi<OfflineState & OfflineActions>>;
