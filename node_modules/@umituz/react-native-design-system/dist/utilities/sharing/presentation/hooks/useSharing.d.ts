/**
 * Sharing Domain - useSharing Hook
 *
 * React hook for sharing files.
 * Provides system share sheet functionality with state management.
 *
 * @domain sharing
 * @layer presentation/hooks
 */
import type { ShareOptions } from '../../domain/entities/Share';
/**
 * useSharing hook for sharing files via system share sheet
 */
export declare const useSharing: () => {
    share: (uri: string, options?: ShareOptions) => Promise<boolean>;
    shareWithAutoType: (uri: string, filename: string, dialogTitle?: string) => Promise<boolean>;
    shareMultiple: (uris: string[], options?: ShareOptions) => Promise<boolean>;
    isAvailable: boolean;
    isSharing: boolean;
    error: string | null;
};
