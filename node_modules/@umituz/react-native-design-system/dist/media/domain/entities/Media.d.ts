/**
 * Media Domain - Core Entities
 *
 * Core types and interfaces for media operations.
 * Handles images, videos, and media library interactions.
 */
/**
 * Media type enumeration
 */
export declare enum MediaType {
    IMAGE = "image",
    VIDEO = "video",
    ALL = "all"
}
/**
 * Image format enumeration
 */
export declare enum ImageFormat {
    PNG = "png",
    JPEG = "jpeg",
    WEBP = "webp"
}
/**
 * Media quality enumeration
 */
export declare enum MediaQuality {
    LOW = 0.3,
    MEDIUM = 0.7,
    HIGH = 1
}
/**
 * Media library permissions
 */
export declare enum MediaLibraryPermission {
    GRANTED = "granted",
    DENIED = "denied",
    LIMITED = "limited"
}
/**
 * Image dimensions interface
 */
export interface ImageDimensions {
    width: number;
    height: number;
}
/**
 * Image manipulation actions
 */
export interface ImageManipulationActions {
    resize?: ImageDimensions;
    crop?: {
        originX: number;
        originY: number;
        width: number;
        height: number;
    };
    rotate?: number;
    flip?: {
        horizontal?: boolean;
        vertical?: boolean;
    };
}
/**
 * Image save options
 */
export interface ImageSaveOptions {
    quality?: MediaQuality;
    format?: ImageFormat;
    base64?: boolean;
}
/**
 * Media picker options
 */
export interface MediaPickerOptions {
    mediaTypes?: MediaType;
    allowsEditing?: boolean;
    allowsMultipleSelection?: boolean;
    aspect?: [number, number];
    quality?: MediaQuality;
    selectionLimit?: number;
    base64?: boolean;
    maxFileSizeMB?: number;
}
/**
 * Media validation error types
 */
export declare enum MediaValidationError {
    FILE_TOO_LARGE = "FILE_TOO_LARGE",
    INVALID_FORMAT = "INVALID_FORMAT",
    PERMISSION_DENIED = "PERMISSION_DENIED",
    PICKER_ERROR = "PICKER_ERROR"
}
/**
 * Media asset interface
 */
export interface MediaAsset {
    uri: string;
    width: number;
    height: number;
    type: MediaType;
    fileSize?: number;
    fileName?: string;
    duration?: number;
    base64?: string;
    mimeType?: string;
}
/**
 * Media picker result
 */
export interface MediaPickerResult {
    canceled: boolean;
    assets?: MediaAsset[];
    error?: MediaValidationError;
    errorMessage?: string;
}
/**
 * Camera options
 */
export interface CameraOptions {
    quality?: MediaQuality;
    allowsEditing?: boolean;
    aspect?: [number, number];
    base64?: boolean;
    videoMaxDuration?: number;
}
/**
 * Media constants
 */
export declare const MEDIA_CONSTANTS: {
    readonly MAX_IMAGE_SIZE_MB: 5;
    readonly MAX_VIDEO_SIZE_MB: 100;
    readonly MAX_IMAGE_SIZE: number;
    readonly MAX_VIDEO_SIZE: number;
    readonly DEFAULT_QUALITY: MediaQuality.HIGH;
    readonly DEFAULT_FORMAT: ImageFormat.JPEG;
    readonly DEFAULT_ASPECT_RATIO: [number, number];
    readonly DEFAULT_SELECTION_LIMIT: 10;
    readonly SUPPORTED_IMAGE_FORMATS: readonly [".jpg", ".jpeg", ".png", ".gif", ".webp"];
    readonly SUPPORTED_VIDEO_FORMATS: readonly [".mp4", ".mov"];
};
