/**
 * File Media Utilities
 * Utilities for handling media files (base64, downloads, file operations)
 */
interface FileWithType {
    readonly type: string;
}
export declare function getMediaDuration(file: FileWithType): Promise<number | undefined>;
export declare function generateThumbnail(file: FileWithType): string | undefined;
/**
 * Check if a string is a base64 data URL
 */
export declare const isBase64DataUrl: (str: string) => boolean;
/**
 * Check if a string is raw base64 (not a URL and not a data URL)
 */
export declare const isRawBase64: (str: string) => boolean;
/**
 * Convert raw base64 to data URL format
 */
export declare const toDataUrl: (str: string) => string;
/**
 * Save base64 image to file system using internal filesystem service
 */
export declare const saveBase64ToFile: (base64Data: string) => Promise<string>;
/**
 * Download media from URL to local file using internal filesystem service
 */
export declare const downloadMediaToFile: (url: string, _isVideo: boolean) => Promise<string>;
export interface SaveToGalleryResult {
    readonly success: boolean;
    readonly error?: string;
}
/**
 * Save image to device gallery
 */
export declare const saveImageToGallery: (uri: string) => Promise<SaveToGalleryResult>;
/**
 * Save video to device gallery
 */
export declare const saveVideoToGallery: (uri: string) => Promise<SaveToGalleryResult>;
export {};
