export interface MediaUrlResult {
    readonly url: string;
    readonly isVideo: boolean;
}
export declare function extractMediaUrl(result: unknown): MediaUrlResult | null;
export declare function extractVideoUrl(result: unknown): string | null;
export declare function extractImageUrl(result: unknown): string | null;
