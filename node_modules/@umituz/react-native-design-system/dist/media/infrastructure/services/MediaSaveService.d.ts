/**
 * Media Save Service
 * Saves media to device storage using expo-file-system
 */
import { MediaLibraryPermission } from "../../domain/entities/Media";
export interface SaveResult {
    success: boolean;
    path?: string;
    error?: string;
}
export interface SaveOptions {
    album?: string;
}
/**
 * Service for saving media to gallery
 */
export declare class MediaSaveService {
    /**
     * Request media library write permission
     */
    static requestPermission(): Promise<MediaLibraryPermission>;
    /**
     * Get current permission status
     */
    static getPermissionStatus(): Promise<MediaLibraryPermission>;
    /**
     * Save image to gallery
     */
    static saveImage(uri: string, options?: SaveOptions): Promise<SaveResult>;
    /**
     * Save video to gallery
     */
    static saveVideo(uri: string, options?: SaveOptions): Promise<SaveResult>;
    /**
     * Save media (image or video) to storage
     */
    private static saveToStorage;
}
