/**
 * Card Component Styles
 */
import { ViewStyle } from 'react-native';
import type { DesignTokens } from '../../../theme';
import type { CardVariant } from '../types';
export declare const getCardVariantStyles: (variant: CardVariant, tokens: DesignTokens) => {
    container: ViewStyle;
};
export declare const cardStyles: {
    container: {
        overflow: "hidden";
        width: "100%";
    };
    header: {
        flexDirection: "row";
        alignItems: "center";
        marginBottom: number;
    };
    titleContainer: {
        flex: number;
    };
    title: {
        fontWeight: "700";
    };
    subtitle: {
        marginTop: number;
    };
    description: {
        marginTop: number;
    };
    image: {
        width: "100%";
    };
    overlay: {
        position: "absolute";
        bottom: number;
        left: number;
        right: number;
        backgroundColor: string;
        padding: number;
    };
    badge: {
        position: "absolute";
        top: number;
        right: number;
        paddingHorizontal: number;
        paddingVertical: number;
        borderRadius: number;
        zIndex: number;
    };
    selectedOverlay: {
        position: "absolute";
        top: number;
        right: number;
        zIndex: number;
    };
};
