/**
 * DatePicker Styles
 * StyleSheet generator for AtomicDatePicker component
 */
import type { DesignTokens } from '../../../theme';
export declare const getDatePickerStyles: (tokens: DesignTokens) => {
    container: {
        marginBottom: number;
    };
    label: {
        fontSize: number;
        fontWeight: "600";
        color: string;
        marginBottom: number;
    };
    errorText: {
        fontSize: number;
        color: string;
        marginTop: number;
        marginLeft: number;
    };
};
