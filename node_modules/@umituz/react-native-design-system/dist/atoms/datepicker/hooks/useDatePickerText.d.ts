/**
 * useDatePickerText Hook
 * Handles date formatting and display text for date picker
 */
import type { DatepickerMode } from '../types';
interface UseDatePickerTextProps {
    value: Date | null;
    placeholder: string;
    mode: DatepickerMode;
}
interface UseDatePickerTextResult {
    displayText: string;
    formatDate: (date: Date) => string;
}
export declare const useDatePickerText: ({ value, placeholder, mode, }: UseDatePickerTextProps) => UseDatePickerTextResult;
export {};
