/**
 * Icon Store - Zustand-based icon configuration
 *
 * No defaults - all icon names come from the app.
 * App sets iconNames and iconRenderer via DesignSystemProvider.
 */
import type { ReactNode } from 'react';
import type { StyleProp, ViewStyle } from 'react-native';
/**
 * Required icon names - app MUST provide all of these
 */
export interface IconNames {
    close: string;
    check: string;
    checkCircle: string;
    chevronLeft: string;
    chevronRight: string;
    chevronUp: string;
    chevronDown: string;
    arrowLeft: string;
    arrowRight: string;
    search: string;
    closeCircle: string;
    clock: string;
    refresh: string;
    alertCircle: string;
    star: string;
    starOutline: string;
    wifi: string;
    wifiOff: string;
    info: string;
    trash: string;
    calendar: string;
    swap: string;
    colorFilter: string;
    diamond: string;
    award: string;
}
export declare const REQUIRED_ICON_KEYS: (keyof IconNames)[];
/**
 * Props passed to the icon renderer function
 */
export interface IconRenderProps {
    name: string;
    size: number;
    color: string;
    style?: StyleProp<ViewStyle>;
    testID?: string;
    accessibilityLabel?: string;
    accessibilityRole?: import('react-native').AccessibilityRole;
}
export type IconRenderer = (props: IconRenderProps) => ReactNode;
interface IconStore {
    iconNames: IconNames | null;
    iconRenderer: IconRenderer | null;
    isConfigured: boolean;
    setConfig: (iconNames: IconNames, iconRenderer: IconRenderer) => void;
    reset: () => void;
}
export declare const useIconStore: import("zustand").UseBoundStore<import("zustand").StoreApi<IconStore>>;
export declare const iconStore: import("zustand").UseBoundStore<import("zustand").StoreApi<IconStore>>;
export declare const DEFAULT_ICON_NAMES: IconNames;
/**
 * Get icon renderer
 */
export declare const useIconRenderer: () => IconRenderer | null;
/**
 * Get specific icon name from app config
 */
export declare const useIconName: (key: keyof IconNames) => string;
/**
 * Check if icon system is configured
 */
export declare const useHasIconConfig: () => boolean;
export {};
