import type { PickerOption } from '../../picker/types';
interface UsePickerStateProps {
    value: string | string[] | null;
    multiple: boolean;
    options: PickerOption[];
    placeholder?: string;
    autoClose?: boolean;
    onChange: (value: string | string[]) => void;
}
export declare const usePickerState: ({ value, multiple, options, placeholder, autoClose, onChange, }: UsePickerStateProps) => {
    modalVisible: boolean;
    searchQuery: string;
    selectedValues: string[];
    selectedOptions: PickerOption[];
    filteredOptions: PickerOption[];
    displayText: string | undefined;
    openModal: () => void;
    closeModal: () => void;
    handleSelect: (optionValue: string) => void;
    handleClear: () => void;
    handleSearch: (query: string) => void;
    handleChipRemove: (value: string) => void;
};
export {};
