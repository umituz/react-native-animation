/**
 * Picker Option Styles
 */
import type { ViewStyle, TextStyle } from 'react-native';
import type { DesignTokens } from '../../../theme';
export declare const getOptionContainerStyles: (tokens: DesignTokens, selected: boolean, disabled: boolean) => ViewStyle;
export declare const getOptionTextStyles: (tokens: DesignTokens, selected: boolean) => TextStyle;
export declare const getOptionDescriptionStyles: (tokens: DesignTokens) => TextStyle;
