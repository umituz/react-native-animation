/**
 * AtomicPicker Styles
 */
import type { ViewStyle, TextStyle } from 'react-native';
import type { DesignTokens } from '../../../theme';
export type PickerSize = 'sm' | 'md' | 'lg';
export interface PickerContainerStyles {
    base: ViewStyle;
    size: Record<PickerSize, ViewStyle>;
    state: {
        error: ViewStyle;
        disabled: ViewStyle;
    };
}
export interface PickerLabelStyles {
    base: TextStyle;
    size: Record<PickerSize, TextStyle>;
}
export interface PickerValueStyles {
    base: TextStyle;
    size: Record<PickerSize, TextStyle>;
}
export interface PickerPlaceholderStyles {
    base: TextStyle;
    size: Record<PickerSize, TextStyle>;
}
export declare const getPickerContainerStyles: (tokens: DesignTokens) => PickerContainerStyles;
export declare const getPickerLabelStyles: (tokens: DesignTokens) => PickerLabelStyles;
export declare const getPickerValueStyles: (tokens: DesignTokens) => PickerValueStyles;
export declare const getPickerPlaceholderStyles: (tokens: DesignTokens) => PickerPlaceholderStyles;
export declare const getPickerErrorStyles: (tokens: DesignTokens) => TextStyle;
export * from './pickerModalStyles';
export * from './pickerSearchStyles';
export * from './pickerOptionStyles';
export * from './pickerStateStyles';
