/**
 * Input Styles Helper
 *
 * Helper functions for AtomicInput component styling.
 * Extracted from AtomicInput for better separation of concerns.
 */
import { ViewStyle } from 'react-native';
import { useAppDesignTokens } from '../../../theme';
import type { AtomicInputVariant, AtomicInputSize } from '../../input/types';
interface GetVariantStyleParams {
    variant: AtomicInputVariant;
    isFocused: boolean;
    hasError: boolean;
    hasSuccess: boolean;
    isDisabled: boolean;
    tokens: ReturnType<typeof useAppDesignTokens>;
}
interface GetSizeConfigParams {
    size: AtomicInputSize;
    tokens: ReturnType<typeof useAppDesignTokens>;
}
export declare const getSizeConfig: ({ size, tokens }: GetSizeConfigParams) => {
    paddingVertical: number;
    paddingHorizontal: number;
    fontSize: number;
    iconSize: number;
    minHeight: number;
} | {
    paddingVertical: number;
    paddingHorizontal: number;
    fontSize: number;
    iconSize: number;
    minHeight: number;
} | {
    paddingVertical: number;
    paddingHorizontal: number;
    fontSize: number;
    iconSize: number;
    minHeight: number;
};
export declare const getVariantStyle: ({ variant, isFocused, hasError, hasSuccess, isDisabled, tokens, }: GetVariantStyleParams) => ViewStyle;
export declare const getTextColor: ({ isDisabled, hasError, hasSuccess, tokens, }: {
    isDisabled: boolean;
    hasError: boolean;
    hasSuccess: boolean;
    tokens: ReturnType<typeof useAppDesignTokens>;
}) => string;
export {};
