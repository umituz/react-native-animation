/**
 * Chip Styles
 */
import type { DesignTokens } from '../../../theme';
import type { ChipVariant } from '../types';
export declare const getChipBorderStyle: (variant: ChipVariant, tokens: DesignTokens) => {
    borderWidth: number;
    borderRadius: number;
};
export declare const getChipSelectedStyle: (selected: boolean, tokens: DesignTokens) => {
    borderWidth?: undefined;
    borderColor?: undefined;
} | {
    borderWidth: number;
    borderColor: string;
};
