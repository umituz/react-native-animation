/**
 * In-Memory Cache
 */
import type { CacheEntry, CacheConfig, CacheStats } from './types/Cache';
import { CacheStatsTracker } from './CacheStatsTracker';
export declare class Cache<T = unknown> {
    protected store: Map<string, CacheEntry<T>>;
    private config;
    protected statsTracker: CacheStatsTracker;
    private strategies;
    constructor(config?: CacheConfig);
    set(key: string, value: T, ttl?: number): void;
    get(key: string): T | undefined;
    has(key: string): boolean;
    delete(key: string): boolean;
    invalidatePattern(pattern: string): number;
    clear(): void;
    getStats(): CacheStats;
    keys(): string[];
    private isExpired;
    private evictOne;
}
