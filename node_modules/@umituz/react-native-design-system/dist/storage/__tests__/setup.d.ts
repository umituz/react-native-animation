/**
 * Test Setup
 *
 * Global test configuration and mocks
 */
import 'jest-environment-jsdom';
export declare const mockPerformance: () => {
    now: jest.Mock<number, [], any>;
    mark: jest.Mock<any, any, any>;
    measure: jest.Mock<any, any, any>;
};
export declare const trackMemoryUsage: () => {
    addListener: (listener: () => void) => () => boolean;
    getListenerCount: () => number;
    cleanup: () => void;
};
