/**
 * useStore Hook
 * Helper for creating stores in components
 */
import type { StoreConfig } from '../../domain/types/Store';
export declare function useStore<T extends object>(config: StoreConfig<T>): import("zustand").UseBoundStore<import("zustand").StoreApi<T>>;
