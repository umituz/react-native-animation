/**
 * useAppInitialization Hook
 * Manages app initialization state in React components
 */
import type { UseAppInitializationOptions, UseAppInitializationReturn, AppInitializerResult } from "./types";
/**
 * Hook to manage app initialization
 *
 * @example
 * ```typescript
 * const { isReady, isLoading, error } = useAppInitialization(initializeApp, {
 * });
 *
 * if (isLoading) return <SplashScreen />;
 * if (error) return <ErrorScreen error={error} />;
 * return <App />;
 * ```
 */
export declare function useAppInitialization(initializer: () => Promise<AppInitializerResult>, options?: UseAppInitializationOptions): UseAppInitializationReturn;
