/**
 * Domain - Filter Entities
 */
export interface FilterOption {
    id: string;
    label: string;
    icon?: string;
    type?: string;
    count?: number;
}
export interface FilterCategory {
    id: string;
    title: string;
    options: FilterOption[];
    multiSelect?: boolean;
}
export declare class FilterUtils {
    static hasActiveFilter(selectedIds: string[], defaultId?: string): boolean;
    static toggleFilter(selectedIds: string[], filterId: string, multiSelect?: boolean, defaultId?: string): string[];
}
