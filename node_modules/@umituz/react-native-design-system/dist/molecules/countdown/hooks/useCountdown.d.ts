import type { TimeRemaining, CountdownTarget } from '../types/CountdownTypes';
export interface UseCountdownOptions {
    interval?: number;
    autoStart?: boolean;
    onExpire?: () => void;
    onTick?: (timeRemaining: TimeRemaining) => void;
}
export interface UseCountdownReturn {
    timeRemaining: TimeRemaining;
    isActive: boolean;
    isExpired: boolean;
    start: () => void;
    stop: () => void;
    reset: () => void;
    setTarget: (target: CountdownTarget) => void;
}
export declare function useCountdown(initialTarget: CountdownTarget | null, options?: UseCountdownOptions): UseCountdownReturn;
