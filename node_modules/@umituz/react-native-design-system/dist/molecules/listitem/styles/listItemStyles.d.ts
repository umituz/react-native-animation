/**
 * ListItem Styles
 */
import type { ViewStyle, TextStyle } from 'react-native';
import type { DesignTokens } from '../../../theme';
export interface ListItemStyles {
    container: ViewStyle;
    disabled: ViewStyle;
    iconContainer: ViewStyle;
    content: ViewStyle;
    subtitle: TextStyle;
}
export declare const getListItemStyles: (tokens: DesignTokens) => ListItemStyles;
