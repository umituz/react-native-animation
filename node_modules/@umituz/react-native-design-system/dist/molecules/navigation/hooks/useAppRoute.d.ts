import type { RouteProp } from "@react-navigation/native";
/**
 * useAppRoute Hook
 *
 * Safe wrapper around React Navigation's useRoute hook.
 * Returns empty route params if called outside NavigationContainer.
 *
 * @example
 * ```typescript
 * const route = useAppRoute();
 * if (route.isReady) {
 *   const params = route.params;
 * }
 * ```
 */
export interface AppRouteResult<T = unknown> {
    key: string;
    name: string;
    params: T | undefined;
    path: string | undefined;
    isReady: boolean;
}
export declare function useAppRoute<T = unknown>(): AppRouteResult<T>;
export type { RouteProp };
