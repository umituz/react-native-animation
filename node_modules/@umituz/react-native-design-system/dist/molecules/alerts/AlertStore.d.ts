/**
 * Alert Store
 */
import { Alert } from './AlertTypes';
interface AlertState {
    alerts: Alert[];
    _updateInProgress: boolean;
}
interface AlertActions {
    addAlert: (alert: Alert) => void;
    dismissAlert: (id: string) => void;
    clearAlerts: () => void;
}
export declare const useAlertStore: import("zustand").UseBoundStore<import("zustand").StoreApi<AlertState & AlertActions>>;
export {};
