/**
 * AlertContent Component
 *
 * Shared title + message layout for alert variants.
 */
import React from 'react';
interface AlertContentProps {
    title: string;
    message?: string;
    titleColor: string;
    messageColor: string;
    titleType?: 'bodyMedium' | 'titleLarge';
    messageType?: 'bodySmall' | 'bodyMedium';
    messageMarginTop?: number;
    titleNumberOfLines?: number;
    messageNumberOfLines?: number;
    textAlign?: 'left' | 'center' | 'right';
}
export declare function AlertContent({ title, message, titleColor, messageColor, titleType, messageType, messageMarginTop, titleNumberOfLines, messageNumberOfLines, textAlign, }: AlertContentProps): React.JSX.Element;
export {};
