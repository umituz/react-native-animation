/**
 * Background Image Collage Component
 * Displays multiple images in various layout patterns with safe area support.
 * Uses expo-image when available, falls back to React Native Image.
 */
import React from "react";
import { type ImageSourceType } from "../../infrastructure/utils/layouts";
export type CollageLayout = "grid" | "dense" | "masonry" | "collage" | "scattered" | "tiles" | "honeycomb";
export interface BackgroundImageCollageProps {
    images: ImageSourceType[];
    layout?: CollageLayout;
    columns?: number;
    gap?: number;
    borderRadius?: number;
    opacity?: number;
}
export declare const BackgroundImageCollage: React.FC<BackgroundImageCollageProps>;
