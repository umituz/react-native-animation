/**
 * Theme Storage
 * Persists theme mode and custom colors using AsyncStorage
 */
import type { ThemeMode } from '../../core/ColorPalette';
import type { CustomThemeColors } from '../../core/CustomColors';
export declare class ThemeStorage {
    static getThemeMode(): Promise<ThemeMode | null>;
    static setThemeMode(mode: ThemeMode): Promise<void>;
    static clearThemeMode(): Promise<void>;
    static getCustomColors(): Promise<CustomThemeColors | undefined>;
    static setCustomColors(colors?: CustomThemeColors): Promise<void>;
    static clearCustomColors(): Promise<void>;
}
