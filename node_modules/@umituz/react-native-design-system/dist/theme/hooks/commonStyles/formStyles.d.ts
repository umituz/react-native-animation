/**
 * Form Styles
 */
import type { ViewStyle } from 'react-native';
import type { DesignTokens } from '../../types/ThemeTypes';
export declare const createFormStyles: (tokens: DesignTokens) => {
    form: ViewStyle;
    formHeader: ViewStyle;
};
