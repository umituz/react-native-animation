/**
 * Padding Styles
 */
import type { ViewStyle } from 'react-native';
import type { DesignTokens } from '../../types/ThemeTypes';
export declare const createPaddingStyles: (tokens: DesignTokens) => {
    paddedHorizontal: ViewStyle;
    paddedVertical: ViewStyle;
    padded: ViewStyle;
};
