/**
 * Section Styles
 */
import type { ViewStyle } from 'react-native';
import type { DesignTokens } from '../../types/ThemeTypes';
export declare const createSectionStyles: (tokens: DesignTokens) => {
    section: ViewStyle;
    sectionPadded: ViewStyle;
};
